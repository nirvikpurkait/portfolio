/**
 * place all cookie name so that it stays consistent accross
 * the application, and this way it will prevent un-intentional
 * typo. it will also help to make some complicated cookie name
 * with simple variable name so that the bowser stores the
 * complicated name insted of simple names.
 */

export const rating = "_ratiG";
